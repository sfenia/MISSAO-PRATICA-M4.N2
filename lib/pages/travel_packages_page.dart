// travel_packages_page.dart
import 'package:flutter/material.dart';

class TravelPackagesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Travel Packages'),
      ),
      body: Center(
        child: const Text('This is the Travel Packages Page'),
      ),
    );
  }
}
