import 'package:flutter/material.dart';

class DestinationsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Destinations'),
      ),
      body: Center(
        child: const Text('This is the Destinations Page'),
      ),
    );
  }
}
